## Requirements
- `openscad` command at `$PATH`

## Build Setup
```
npm install
npm start
```

## Add model module
- Place to `src/models/modelName.js`
- Export `{ name, label, generator, params, presets }`
